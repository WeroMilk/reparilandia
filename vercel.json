{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "(test -d repariland-next && npm ci --prefix repariland-next && npm ci) || npm ci",
  "buildCommand": "(test -d repariland-next && npm run build --prefix repariland-next) || npm run build"
}
